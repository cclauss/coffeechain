#!/bin/bash
IFS=$'\n'
GETKEYS=`node -e "console.log(require('zeromq').curveKeypair())"`
i=0
KEYS[0]=""
KEYS[1]=""

for key in $GETKEYS
do
	KEYS[$i]=$([[ $key =~ \'(.*?)\' ]] && echo $BASH_REMATCH)
	((i++))
done	

export KEYS
echo ${KEYS[*]} >> keypair.txt 
