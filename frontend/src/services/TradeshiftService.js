var Promise = require('promise-polyfill')

export default {
  fetchTradeShiftInfos: function () {
    return new Promise(function (resolve, reject) {
      let blob = {}
      resolve(blob)
    })
  }
}
